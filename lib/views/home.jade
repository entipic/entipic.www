extends layouts/root

block content
  .row
    .col-xs-12.col-sm-8
      h1 A picture for each entity!
      p Entipic.com is a service offering every entity/topic (i.e., Google, Barack Obama, Paris, etc.) a representative image that can be freely used by any application.
      br
      h4 Examples:
      #examples.well.well-small
        .row
          each item in exampleTopics
            .col-xs-6.col-sm-4
              .media
                .media-left
                  img.media-object(src=entipicUrl.picture(item.pictureId))
                .media-body &larr; 
                  b
                    a(href=links.topic(item.uniqueName))= item.name

      //- p.stats In last 
      //-   span.count 7 days 
      //-   | we have explored 
      //-   span.count= countTopics7Days.toLocaleString()
      //-   |  topics, 
      //-   span.count= countUniqueNames7Days.toLocaleString()
      //-   |  unique names and 
      //-   span.count= countPictures7Days.toLocaleString()
      //-   |  pictures.
      //- br
      p.stats Entipic knows 
        span.count= countTopics.toLocaleString()
        |  topics, 
        span.count= countUniqueNames.toLocaleString()
        |  unique names and 
        span.count= countPictures.toLocaleString()
        |  pictures.

    .col-xs-12.col-sm-4
      .panel.panel-default
        .panel-heading
          h3.panel-title Latest entities
        .panel-body.text-center.stats
          ul.list.ot-picture.view-icon
            each item in latestTopics
              li
                a(href=links.topic(item.uniqueName), title=item.name)
                  img(src=entipicUrl.picture(item.pictureId), alt='')
