extends layouts/root

block content
  .row
    .col.xs-12.col-sm-3
      #picture
        img(src=entipicUrl.picture(topic.pictureId, 'd'), alt=topic.name, class='img-responsive')

    .col-xs-12.col-sm-9
      h1= topic.name
      //- if (topic.englishWikiDescription)
      p= topic.description
      p= topic.type
      p= topic.refHost

  br

  .row
    .col.xs-12.col-sm-3
      h4 Pictures:
      ul.list.ot-picture
        each item in topic.picturesIds
          li
            img(src=entipicUrl.picture(item), alt=topic.name)

    .col-xs-12.col-sm-9
      //- h4 Unique names:
      //- ul.list.ot-un
      //-   each item in uniqueNames
      //-     li
      //-       div= item.name
      //-         |  (
      //-         = item.locale
      //-         |)
      //-       div
      //-         b= item.uniqueName
